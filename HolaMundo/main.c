#include <stdio.h> //libreria para entrada y salida de datos (input and output)
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	int numero = 0; //declar numeros enteros

	char myName[] = "Hector Daniel Hernandez Castillo";
	


	
	
	
	printf("Hola %s", myName); //se imprime el mensaje :V 
	getch(); //detiene la pantalla 
	return 0; //retorna 0
}
